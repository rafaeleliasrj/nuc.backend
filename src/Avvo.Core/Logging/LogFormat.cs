namespace Avvo.Core.Logging
{
    /// <summary>
    /// This enumeration is used to define supported log formats for the Logger.
    /// </summary>
    public enum LogFormat
    {
        TEXT,
        JSON
    }
}

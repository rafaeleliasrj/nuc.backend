services:
  api:
    build:
      context: ./backend
      dockerfile: ./src/Avvo.API/Dockerfile
    container_name: nuc_api
    ports:
      - "5000:5000"   # acesso externo
    depends_on:
      - db

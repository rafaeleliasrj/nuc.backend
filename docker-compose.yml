services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nuc_api
    ports:
      - "5000:5000"   # acesso externo
    depends_on:
      - db
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=db;Database=nucdb;User=root;Password=example;
